<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/addAccountCard"
        android:layout_width="330dp"
        android:layout_height="180dp"
        android:background="@drawable/bg_account_card"
        android:clickable="true"
        android:clipToOutline="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5" />

    <ImageView
        android:id="@+id/plusIcon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintBottom_toBottomOf="@+id/addAccountCard"
        app:layout_constraintEnd_toEndOf="@+id/addAccountCard"
        app:layout_constraintStart_toStartOf="@+id/addAccountCard"
        app:layout_constraintTop_toTopOf="@+id/addAccountCard"
        app:srcCompat="@drawable/ic_plus_solid_full" />

</androidx.constraintlayout.widget.ConstraintLayout>